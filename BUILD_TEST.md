# Build test

The GitHub Actions Windows build publishes a self-contained `win-x64` Farla Tweaks package as `FarlaTweaks-Windows-x64`.

The current milestone is the minimal application shell. Do not treat the project as feature-complete until the Windows artifact builds and launches successfully.
