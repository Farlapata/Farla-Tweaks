<Application x:Class="FarlaTweaks.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="FarlaBackground" Color="#0A0C10" />
        <SolidColorBrush x:Key="FarlaSurface" Color="#10141B" />
        <SolidColorBrush x:Key="FarlaSurfaceAlt" Color="#151A22" />
        <SolidColorBrush x:Key="FarlaBorder" Color="#242B35" />
        <SolidColorBrush x:Key="FarlaText" Color="#F2F4F7" />
        <SolidColorBrush x:Key="FarlaMuted" Color="#8B93A1" />
        <SolidColorBrush x:Key="FarlaAccent" Color="#A4BFA7" />
        <SolidColorBrush x:Key="FarlaAccentSoft" Color="#6F826F" />
        <SolidColorBrush x:Key="FarlaSuccess" Color="#A4BFA7" />
        <SolidColorBrush x:Key="FarlaWarning" Color="#CDB77B" />
        <SolidColorBrush x:Key="FarlaDanger" Color="#C98282" />

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource FarlaMuted}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource FarlaText}" />
                    <Setter Property="Background" Value="#151A22" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="#1A211B" />
            <Setter Property="Foreground" Value="{StaticResource FarlaText}" />
            <Setter Property="BorderBrush" Value="{StaticResource FarlaAccentSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="16,10" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#222C23" />
                    <Setter Property="BorderBrush" Value="{StaticResource FarlaAccent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
