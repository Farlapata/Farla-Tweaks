<Window x:Class="FarlaTweaks.App.DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Width="900"
        Height="620"
        WindowStartupLocation="CenterOwner">
    <Border Background="{StaticResource FarlaBackground}" BorderBrush="{StaticResource FarlaBorder}" BorderThickness="1" CornerRadius="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
                <RowDefinition Height="*" />
                <RowDefinition Height="68" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" MouseLeftButtonDown="DragArea_OnMouseLeftButtonDown">
                <StackPanel VerticalAlignment="Center" Margin="26,0,0,0">
                    <TextBlock Text="DIAGNOSTICS" Foreground="{StaticResource FarlaText}" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Read-only Windows health checks." Foreground="{StaticResource FarlaMuted}" FontSize="12" Margin="0,3,0,0" />
                </StackPanel>
                <Button Content="×" Width="42" Height="32" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0" Style="{StaticResource NavButtonStyle}" Click="CloseButton_OnClick" />
            </Grid>
            <StackPanel Grid.Row="1" Margin="26,10,26,20">
                <TextBlock x:Name="StatusText" Text="No checks have been run." Foreground="{StaticResource FarlaMuted}" FontSize="12" />
                <StackPanel x:Name="ResultList" Margin="0,18,0,0" />
            </StackPanel>
            <Border Grid.Row="2" Background="#0B0E12" BorderBrush="{StaticResource FarlaBorder}" BorderThickness="0,1,0,0">
                <Grid Margin="26,0">
                    <TextBlock Text="Checks may take a few minutes on some Windows installations."
                               Foreground="{StaticResource FarlaMuted}"
                               FontSize="11"
                               VerticalAlignment="Center" />
                    <Button x:Name="RunButton" Content="RUN CHECKS" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="RunButton_OnClick" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
