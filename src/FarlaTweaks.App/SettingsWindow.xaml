<Window x:Class="FarlaTweaks.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Width="760"
        Height="620"
        WindowStartupLocation="CenterOwner">
    <Border Background="{StaticResource FarlaBackground}" BorderBrush="{StaticResource FarlaBorder}" BorderThickness="1" CornerRadius="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
                <RowDefinition Height="*" />
                <RowDefinition Height="68" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" MouseLeftButtonDown="DragArea_OnMouseLeftButtonDown">
                <StackPanel VerticalAlignment="Center" Margin="26,0,0,0">
                    <TextBlock Text="SETTINGS" Foreground="{StaticResource FarlaText}" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Farla preferences and local data" Foreground="{StaticResource FarlaMuted}" FontSize="12" Margin="0,3,0,0" />
                </StackPanel>
                <Button Content="×" Width="42" Height="32" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0" Style="{StaticResource NavButtonStyle}" Click="CloseButton_OnClick" />
            </Grid>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                <StackPanel Margin="26,8,26,24">
                    <TextBlock Text="YOUR SETUP" Foreground="{StaticResource FarlaMuted}" FontSize="11" FontWeight="SemiBold" />
                    <Border Background="{StaticResource FarlaSurface}" BorderBrush="{StaticResource FarlaBorder}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,10,0,0">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="Setup Wizard" Foreground="{StaticResource FarlaText}" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Text="Change your game, goals, and software dependencies." Foreground="{StaticResource FarlaMuted}" FontSize="12" Margin="0,4,0,0" />
                            </StackPanel>
                            <Button Content="OPEN WIZARD" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource PrimaryButtonStyle}" Click="OpenWizardButton_OnClick" />
                        </Grid>
                    </Border>

                    <Border Background="{StaticResource FarlaSurface}" BorderBrush="{StaticResource FarlaBorder}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,10,0,0">
                        <Grid>
                            <StackPanel>
                                <TextBlock Text="System diagnostics" Foreground="{StaticResource FarlaText}" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Text="Run read-only Windows integrity checks." Foreground="{StaticResource FarlaMuted}" FontSize="12" Margin="0,4,0,0" />
                            </StackPanel>
                            <Button Content="OPEN DIAGNOSTICS" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource NavButtonStyle}" Click="OpenDiagnosticsButton_OnClick" />
                        </Grid>
                    </Border>

                    <TextBlock Text="LOCAL DATA" Foreground="{StaticResource FarlaMuted}" FontSize="11" FontWeight="SemiBold" Margin="0,24,0,0" />
                    <Border Background="{StaticResource FarlaSurface}" BorderBrush="{StaticResource FarlaBorder}" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="Farla stores system profile, preferences, and rollback snapshots locally." Foreground="{StaticResource FarlaText}" FontSize="13" TextWrapping="Wrap" />
                            <TextBlock Text="%LOCALAPPDATA%\Farla" Foreground="{StaticResource FarlaMuted}" FontSize="12" Margin="0,7,0,0" />
                        </StackPanel>
                    </Border>

                    <TextBlock Text="RELEASE" Foreground="{StaticResource FarlaMuted}" FontSize="11" FontWeight="SemiBold" Margin="0,24,0,0" />
                    <Grid Margin="0,10,0,0">
                        <TextBlock Text="Farla Tweaks Alpha 0.1" Foreground="{StaticResource FarlaText}" FontSize="13" />
                        <TextBlock Text="Windows x64" Foreground="{StaticResource FarlaMuted}" FontSize="12" HorizontalAlignment="Right" />
                    </Grid>
                </StackPanel>
            </ScrollViewer>
            <Border Grid.Row="2" Background="#0B0E12" BorderBrush="{StaticResource FarlaBorder}" BorderThickness="0,1,0,0">
                <Grid Margin="26,0">
                    <TextBlock Text="Commercial release will add account/licensing controls here."
                               Foreground="{StaticResource FarlaMuted}"
                               FontSize="11"
                               VerticalAlignment="Center" />
                    <Button Content="DONE" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="CloseButton_OnClick" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
