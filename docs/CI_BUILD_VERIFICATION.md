# CI Build Verification

This file exists only to force a fresh Windows CI run against the current `main` codebase.

The required milestone is a successful `win-x64` self-contained build that produces the `FarlaTweaks-Windows-x64` artifact.
